#include <stdio.h>

main() {
	int a, b, n = 0, m = 0;
	scanf("%d", &b);
	
	for(a = 3;a <= b;a++){
		if(b % a == 0)
			n++;
	}
	for(a = 5;a <= b;a++){
		if(b % a == 0)
			m++;
	}
	if(n<m)
		printf("%d", n);
	else printf("%d", m); 
}
